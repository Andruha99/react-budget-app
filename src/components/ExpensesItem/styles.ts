import styled from "styled-components";

const ExpensesItemWrapper = styled.div`
  display: flex;
  justify-content: space-between;
  border-bottom: 2px solid #CCD5FF;
`;

export { ExpensesItemWrapper } ;