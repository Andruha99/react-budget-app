import React from "react"
import { ExpensesList } from "../ExpensesList/ExpensesList"

export const Expenses = () => {
  return (
    <div>
      <h2>Expenses</h2>
      <ExpensesList />
    </div>
  )
}
